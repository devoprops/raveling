[phases.setup]
nixPkgs = ["python313", "curl"]

[phases.install]
cmds = [
  "curl -LsSf https://astral.sh/uv/install.sh | sh",
  "export PATH=\"$HOME/.local/bin:$PATH\"",
  "# Sync dependencies - pyproject.toml constrains bcrypt to <5.0.0",
  "uv sync"
]

[start]
cmd = "bash start.sh"

